name: GitHub Actions Demo
run-name: ${{ github.actor }} is testing out GitHub Actions 🚀

on:
  push:
    branches: 
      - main

jobs:
  pre-deploy:
    runs-on: ubuntu-latest
    steps:
      - run: echo "🎉 The job was automatically triggered by a ${{ github.event_name }} event."
      - run: echo "🐧 This job is now running on a ${{ runner.os }} server hosted by GitHub!"
      - run: echo "🔎 The name of your branch is ${{ github.ref }} and your repository is ${{ github.repository }}."

  install-dependencies:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository code
        uses: actions/checkout@v3
      - name: Run Installation of Dependencies Commands
        run: npm install
      - name: Run Test Commands
        run: npm test
      - run: echo "🍏 This job's status is ${{ job.status }}."

  deploy:
    name: deploy
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: [14.x]
    steps:
      - uses: actions/checkout@v2
      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v1
        with:
          node-version: ${{ matrix.node-version }}
      - run: npm install
      - run: node ./node_modules/serverless/bin/serverless.js config credentials --provider aws --key AKIATXF4JQPHRY53OCNL --secret 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
      - run: node ../../node_modules/serverless/bin/serverless.js deploy
        working-directory: ./serverless/challenge-cicd-serverless
        env:
          AWS_ACCESS_KEY_ID: AKIATXF4JQPHRY53OCNL
          AWS_SECRET_ACCESS_KEY: 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
          SLS_DEBUG: 1